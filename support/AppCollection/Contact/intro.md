# Simplic contact system

The simplic contact systems is the core of the simplic crm. All contact informations are stored in the tables that starts with `IT_Contacts_`.
The stack name is `STACK_Contact`.